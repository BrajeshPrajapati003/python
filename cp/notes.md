**String operations**

- ch.isDigit() : digit chars (includes unicode digits)
- ch.isNumeric() : even broader numeric symbols
- ch.isDecimal() : strict decimal digits (0-9 only)
- if '0' <= ch <= '9': # check ASCII digits only
    print("Digit")
- Integer.parseInt(str, radix) = int()
- String.valueOf() = str()
